import './Spinner.styles.scss';

/**
 * Spinning Loader component
 * Just a simple spinning loader displayed.
 */
const Spinner = () => (
	<div className='spinner'>
		<div className='spinner-container' />
	</div>
);

export default Spinner;
